# Copy this file to `.env` in the project root. Vite only loads `.env` / `.env.local` — not `.env.example`.
# After creating or editing `.env`, restart `npm run dev` (env is read at server start).

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=
VITE_CONTACT_TO_EMAIL=
